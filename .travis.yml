script:
- "build/deploy.rb"

language: ruby
before_install:

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in travis-ci.com dashboard, marked secure
  keep-history: true
  on:
    branch: master
