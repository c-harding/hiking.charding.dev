!!!5
%html{lang: "en"}
  %head
    %meta{charset: "utf-8"}/
    %link{rel: 'canonical', href: '/'}/
    %title Hiking with Charlie
    %meta{name: 'viewport', content: 'width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no'}
    -#
      %meta{'http-equiv': 'Refresh', content: '0; url=https://xsanda.me'}
    %link{rel: 'stylesheet', href: 'https://fonts.googleapis.com/css?family=Satisfy', type: 'text/css'}
    %link{rel: 'stylesheet', href: '/style/listing.css', type: 'text/css'}
    %script{src: 'https://kit.fontawesome.com/772a32a5d5.js'}
  %body
    %main
      %heading
        %h1 Hiking with Charlie
        %p
          organised through
          %a.hb-logo{href: 'https://www.hiking-buddies.com/routes/'} Hiking Buddies
      - hikes.each do |hike|
        %a.card{href: hike.local_link}
          %figure
            %img{src: hike.image}
          %aside
            %h2= hike.title
            %ul
              %li= hike.category_icon
              - hike.tags.each do |tag|
                %li= tag
              - if hike.distance
                %li
                  ↔︎
                  = hike.distance
              - if hike.ascent
                %li
                  ↗︎
                  = hike.ascent
              - if hike.grade
                %li= hike.grade
            %ul{data: {id: hike.id}}
              %li= hike.day_date_time_string
              - if hike.past?
                %li #{hike.registered} registered
              - elsif hike.available > 0
                %li.capacity.available
                  #{hike.registered}/#{hike.capacity} registered,
                  #{hike.available} #{'space'.pluralize(hike.available)} available
              - else
                %li.capacity.full #{hike.registered}/#{hike.capacity} registered, #{hike.waiting} waiting
            %p= hike.desc